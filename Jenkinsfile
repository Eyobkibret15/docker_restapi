pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                env.DEBUG= "${DEBUG}"
                echo 'Hello World'
            }
        }
        stage('test') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
